<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/style.css' | url }}" />
  </head>
  <body>
    <header>
      <img
        class="logo"
        src="{{ '/images/logo.png' | url }}"
        alt="minimal logo reading 'Photo To Citation' with camera and ticket icon, dark background"
        width="40"
        height="40"
        data-image-gen='{"model":"gpt-image-1","size":"1024x1024"}'
      />
      <nav>
        <a href="{{ '/' | url }}">Home</a>
        <a href="{{ '/features/' | url }}">Features</a>
        <a href="{{ '/about/' | url }}">About</a>
      </nav>
    </header>
    <main>
      {{ content | safe }}
    </main>
  </body>
</html>
